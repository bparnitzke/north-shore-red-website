# No runtime secrets are required for the first-phase static marketing site.
# Future voter-plan or SMS integrations must define approved server-side
# environment variables here, never committed secret values.
